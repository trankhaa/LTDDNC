@* @model backend.Models.Entities.Packages

@{
    ViewData["Title"] = "Package Details";
    Layout = "_Layout";
}

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">@ViewData["Title"]</h6>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-2">Package Name:</dt>
            <dd class="col-sm-10">@Model.PackageName</dd>

            <dt class="col-sm-2">Description:</dt>
            <dd class="col-sm-10">@Model.Description</dd>

            <dt class="col-sm-2">Price:</dt>
            <dd class="col-sm-10">@Model.Price.ToString("C")</dd>

            <dt class="col-sm-2">Duration:</dt>
            <dd class="col-sm-10">@Model.DurationMinutes minutes</dd>

            <dt class="col-sm-2">Specialty:</dt>
            <dd class="col-sm-10">@Model.Specialty?.SpecialtyName</dd>

            <dt class="col-sm-2">Created At:</dt>
            <dd class="col-sm-10">@Model.CreatedAt.ToString("g")</dd>

            <dt class="col-sm-2">Updated At:</dt>
            <dd class="col-sm-10">@Model.UpdatedAt.ToString("g")</dd>
        </dl>

        <div>
            <a asp-action="Edit" asp-route-id="@Model.IdPackage" class="btn btn-warning">Edit</a>
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
</div> *@