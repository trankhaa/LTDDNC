@model backend.Models.Entities.Specialty

@{
    ViewData["Title"] = "Delete Specialty";
}

<h1>Delete Specialty</h1>

<h3>Are you sure you want to delete this specialty?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.SpecialtyName)</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Description)</dd>

        <dt class="col-sm-2">Department</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Department != null ? model.Department.DepartmentName : string.Empty)</dd>

        @if (!string.IsNullOrEmpty(Model.ImageUrl))
        {
            <dt class="col-sm-2">Image</dt>
            <dd class="col-sm-10">
                <img src="@Model.ImageUrl" alt="@Model.SpecialtyName" style="max-width: 200px;" />
            </dd>
        }
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="IdSpecialty" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </form>
</div>